controller_manager:
  ros__parameters:
    update_rate: 500

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    arm_controller:
      type: joint_trajectory_controller/JointTrajectoryController

    gripper_controller:
      type: parallel_gripper_action_controller/GripperActionController

joint_state_broadcaster:
  ros__parameters:
    joints:
      - J1
      - J2
      - J3
      - J4
      - J5
      - J6
      - robotiq_85_left_knuckle_joint

arm_controller:
  ros__parameters:
    joints:
      - J1
      - J2
      - J3
      - J4
      - J5
      - J6
    command_interfaces:
      - position
    state_interfaces:
      - position
      - velocity
    state_publish_rate: 100.0
    action_monitor_rate: 20.0
    allow_partial_joints_goal: false

gripper_controller:
  ros__parameters:
    joint: robotiq_85_left_knuckle_joint
    # NOTE: position_controllers/GripperActionController is deprecated in Jazzy and
    # segfaults on activate; parallel_gripper_action_controller is the working replacement.
    state_interfaces:
      - position
      - velocity
    allow_stalling: true
    stall_timeout: 0.05
    goal_tolerance: 0.003
